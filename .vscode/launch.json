{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug X12 (CodeLLDB)",
            "program": "${workspaceFolder}/x12", 
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "manual-dub-build"
        }
    ]
}